{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "scope-enum": [
      2,
      "always",
      [
        "playwright-test-auth",
        "hcc-page-objects",
        "release",
        "deps",
        "ci"
      ]
    ]
  }
}
